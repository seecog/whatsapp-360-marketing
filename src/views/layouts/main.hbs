<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - mini-hr-360</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/flatpickr/dist/themes/material_blue.css" rel="stylesheet">
    <script src="https://cdn.ckeditor.com/ckeditor5/40.1.0/classic/ckeditor.js"></script>
    <style>
        /* ---------- App shell (Dashboard-style pages) ---------- */
        .sidebar {
            min-height: 100vh;
            background: linear-gradient(180deg, #111827 0%, #0b1220 100%);
            color: rgba(255, 255, 255, 0.9);
            box-shadow: inset 0 0 0 1px rgba(255,255,255,0.05);
            overflow-y: auto;
        }
        .sidebar .nav-link {
            color: rgba(255, 255, 255, 0.78);
            padding: 0.70rem 0.85rem;
            margin: 0.15rem 0;
            border-radius: 0.75rem;
            transition: background-color 120ms ease, color 120ms ease;
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 600;
            font-size: 14px;
        }
        .sidebar .nav-link:hover {
            color: #fff;
            background: rgba(255, 255, 255, 0.08);
        }
        .sidebar .nav-link.active {
            color: #fff;
            background: rgba(99, 102, 241, 0.22);
            box-shadow: inset 0 0 0 1px rgba(99, 102, 241, 0.35);
        }
        .sidebar .nav-link i {
            width: 18px;
            text-align: center;
            opacity: 0.95;
        }
        .sidebar .submenu {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease-out;
        }
        /* IMPORTANT FIX: remove the 200px cap so all items (e.g., Business) are visible */
        .sidebar .submenu.show {
            max-height: none;            /* no clipping */
            overflow: visible;           /* let items be fully visible */
            transition: max-height 0.3s ease-in;
        }
        .sidebar .submenu .nav-link {
            padding-left: 2.5rem;
            font-size: 0.9rem;
        }
        .sidebar .menu-toggle {
            cursor: pointer;
        }
        .sidebar .menu-toggle i.fa-chevron-down {
            transition: transform 0.3s ease;
        }
        .sidebar .menu-toggle.collapsed i.fa-chevron-down {
            transform: rotate(-90deg);
        }

        .sidebar-brand {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        .brand-mark {
            width: 40px;
            height: 40px;
            border-radius: 12px;
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            display: grid;
            place-items: center;
            color: #fff;
            box-shadow: 0 12px 26px rgba(99, 102, 241, 0.25);
        }
        .brand-title {
            font-weight: 800;
            letter-spacing: -0.01em;
            font-size: 16px;
            color: #fff;
            line-height: 1.1;
        }
        .brand-subtitle {
            font-size: 11px;
            letter-spacing: 0.12em;
            color: rgba(255,255,255,0.55);
            margin-top: 2px;
        }
        .sidebar-profile {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 10px;
            border-radius: 14px;
            background: rgba(255,255,255,0.05);
            box-shadow: inset 0 0 0 1px rgba(255,255,255,0.06);
        }
        .profile-avatar {
            width: 44px;
            height: 44px;
            border-radius: 999px;
            background: rgba(255,255,255,0.10);
            display: grid;
            place-items: center;
            color: rgba(255,255,255,0.9);
        }
        .profile-name {
            font-weight: 700;
            font-size: 13px;
            color: rgba(255,255,255,0.92);
            line-height: 1.1;
        }
        .profile-role {
            display: inline-block;
            margin-top: 6px;
            padding: 5px 10px;
            border-radius: 999px;
            background: rgba(255,255,255,0.08);
            color: rgba(255,255,255,0.75);
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 0.12em;
        }
        .sidebar-divider {
            height: 1px;
            background: rgba(255,255,255,0.08);
            margin: 10px 0;
            border-radius: 1px;
        }
        
        /* Fancy Calendar Styling */
        .fancy-datepicker {
            position: relative;
        }
        .fancy-datepicker .form-control {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border: 2px solid #dee2e6;
            border-radius: 12px;
            padding: 12px 45px 12px 15px;
            font-size: 14px;
            font-weight: 500;
            color: #495057;
            transition: all 0.3s ease;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        .fancy-datepicker .form-control:focus {
            border-color: #ffd700;
            box-shadow: 0 0 0 0.2rem rgba(255, 215, 0, 0.25);
            background: linear-gradient(135deg, #fff 0%, #f8f9fa 100%);
        }
        .fancy-datepicker .date-icon {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #ffd700;
            font-size: 18px;
            pointer-events: none;
            z-index: 10;
        }
        .fancy-datepicker .form-label {
            font-weight: 600;
            color: #495057;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .fancy-datepicker .form-label i {
            color: #ffd700;
            font-size: 16px;
        }
        
        /* Flatpickr Custom Styling */
        .flatpickr-calendar {
            border-radius: 15px !important;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2) !important;
            border: none !important;
            font-family: inherit !important;
        }
        .flatpickr-months {
            background: linear-gradient(135deg, #ffd700 0%, #ffb347 100%) !important;
            border-radius: 15px 15px 0 0 !important;
        }
        .flatpickr-month {
            color: #000 !important;
            font-weight: 600 !important;
        }
        .flatpickr-prev-month, .flatpickr-next-month {
            color: #000 !important;
        }
        .flatpickr-prev-month:hover, .flatpickr-next-month:hover {
            color: #333 !important;
        }
        .flatpickr-day.selected {
            background: linear-gradient(135deg, #ffd700 0%, #ffb347 100%) !important;
            border-color: #ffd700 !important;
            color: #000 !important;
            font-weight: 600 !important;
        }
        .flatpickr-day:hover {
            background: rgba(255, 215, 0, 0.2) !important;
            border-color: #ffd700 !important;
        }
        .flatpickr-day.today {
            border-color: #ffd700 !important;
            color: #ffd700 !important;
            font-weight: 600 !important;
        }
        .main-content {
            background: #eef1f7;
            min-height: 100vh;
        }
        .page-title {
            font-weight: 800;
            letter-spacing: -0.02em;
            color: #0f172a;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .page-title::before {
            content: "";
            width: 4px;
            height: 26px;
            border-radius: 999px;
            background: linear-gradient(180deg, #6366f1 0%, #8b5cf6 100%);
        }
        .page-subtitle {
            color: rgba(15, 23, 42, 0.62);
            margin-top: 4px;
        }

        .stat-grid {
            display: grid;
            grid-template-columns: repeat(7, minmax(0, 1fr));
            gap: 14px;
        }
        @media (max-width: 1400px) {
            .stat-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }
        }
        @media (max-width: 992px) {
            .stat-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
        }
        .stat-card {
            background: #fff;
            border-radius: 14px;
            padding: 14px 14px 12px;
            box-shadow: 0 14px 34px rgba(15, 23, 42, 0.06);
            border: 1px solid rgba(15, 23, 42, 0.06);
            cursor: pointer;
            transition: transform 140ms ease, box-shadow 140ms ease;
            text-align: center;
        }
        .stat-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 20px 44px rgba(15, 23, 42, 0.10);
        }
        .stat-icon {
            width: 44px;
            height: 44px;
            border-radius: 14px;
            margin: 0 auto 10px;
            display: grid;
            place-items: center;
            color: #fff;
            box-shadow: 0 12px 26px rgba(15, 23, 42, 0.12);
        }
        .stat-label {
            font-size: 11px;
            letter-spacing: 0.08em;
            font-weight: 800;
            color: rgba(15, 23, 42, 0.55);
            text-transform: uppercase;
            margin-bottom: 2px;
        }
        .stat-value {
            font-size: 24px;
            font-weight: 900;
            color: #0f172a;
            line-height: 1.1;
        }
        .stat-link {
            margin-top: 6px;
            font-size: 12px;
            font-weight: 700;
            color: rgba(99, 102, 241, 0.95);
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }

        .section-card {
            background: #fff;
            border-radius: 16px;
            border: 1px solid rgba(15, 23, 42, 0.06);
            box-shadow: 0 14px 34px rgba(15, 23, 42, 0.06);
            overflow: hidden;
        }
        .section-header {
            padding: 14px 16px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: rgba(15, 23, 42, 0.02);
            border-bottom: 1px solid rgba(15, 23, 42, 0.06);
        }
        .section-header h5 {
            margin: 0;
            font-weight: 800;
            color: #0f172a;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }
        .btn-viewall {
            border: none;
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            color: #fff;
            padding: 8px 14px;
            border-radius: 12px;
            font-weight: 800;
            font-size: 12px;
            box-shadow: 0 12px 26px rgba(99, 102, 241, 0.20);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        .btn-viewall:hover { filter: brightness(0.98); color: #fff; }

        /* ---------- DataTable-like UI helpers (tables) ---------- */
        .table-premium thead th {
            border-bottom: 1px solid rgba(15, 23, 42, 0.08);
            background: rgba(15, 23, 42, 0.02);
            color: rgba(15, 23, 42, 0.62);
            font-weight: 900;
        }
        .table-premium tbody tr:hover {
            background: rgba(99, 102, 241, 0.04);
        }
        .th-sort {
            cursor: pointer;
            user-select: none;
        }
        .th-sort:hover {
            color: rgba(15, 23, 42, 0.85);
        }
        .th-sort .sort-ind {
            font-weight: 900;
            color: rgba(99, 102, 241, 0.9);
        }

        .btn-icon {
            width: 34px;
            height: 34px;
            border-radius: 12px;
            padding: 0;
            display: inline-grid;
            place-items: center;
            border: 1px solid rgba(15, 23, 42, 0.08);
            background: #fff;
            box-shadow: 0 10px 22px rgba(15, 23, 42, 0.06);
            transition: transform 140ms ease, box-shadow 140ms ease, filter 140ms ease;
        }
        .btn-icon:hover {
            transform: translateY(-1px);
            box-shadow: 0 16px 30px rgba(15, 23, 42, 0.10);
        }
        .btn-icon i {
            font-size: 14px;
        }
        .btn-icon.edit {
            background: linear-gradient(135deg, rgba(99, 102, 241, 0.16), rgba(139, 92, 246, 0.14));
            border-color: rgba(99, 102, 241, 0.22);
            color: rgba(55, 48, 163, 0.95);
        }
        .btn-icon.delete {
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.14), rgba(249, 115, 22, 0.12));
            border-color: rgba(239, 68, 68, 0.22);
            color: rgba(185, 28, 28, 0.95);
        }
        .card {
            border: none;
            border-radius: 1rem;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
        }
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
        }
        .btn-primary:hover {
            background: linear-gradient(135deg, #5a6fd8 0%, #6a4190 100%);
        }
        .alert {
            border: none;
            border-radius: 0.75rem;
        }
        .ck-editor__editable {
            min-height: 200px;
        }
        .ck-editor__main {
            border-radius: 0.375rem;
        }

        /* ---------- Premium Auth Pages (scoped) ---------- */
        body.auth {
            background:
                radial-gradient(1200px 600px at 50% -10%, rgba(24, 64, 255, 0.14), rgba(255, 255, 255, 0)),
                radial-gradient(900px 500px at 110% 20%, rgba(118, 75, 162, 0.14), rgba(255, 255, 255, 0)),
                linear-gradient(180deg, #f7f8fb 0%, #eef1f7 100%);
        }

        .auth-shell {
            position: relative;
            overflow: hidden;
        }

        /* Floating icon decorations (auth pages) */
        .auth-decor {
            position: absolute;
            inset: 0;
            pointer-events: none;
            z-index: 0;
        }

        .auth-float {
            position: absolute;
            width: 62px;
            height: 62px;
            border-radius: 16px;
            display: grid;
            place-items: center;
            box-shadow:
                0 18px 40px rgba(15, 23, 42, 0.10),
                0 2px 10px rgba(15, 23, 42, 0.06);
            animation: authFloat 6.5s ease-in-out infinite;
            opacity: 0.95;
            transform: translateZ(0);
        }

        .auth-float i {
            color: #fff;
            font-size: 22px;
        }

        .auth-float.c1 { background: linear-gradient(135deg, #22c55e, #10b981); }
        .auth-float.c2 { background: linear-gradient(135deg, #8b5cf6, #6366f1); }
        .auth-float.c3 { background: linear-gradient(135deg, #ec4899, #f43f5e); }
        .auth-float.c4 { background: linear-gradient(135deg, #f59e0b, #f97316); }
        .auth-float.c5 { background: linear-gradient(135deg, #06b6d4, #3b82f6); }

        .auth-float.s1 { width: 70px; height: 70px; border-radius: 18px; }
        .auth-float.s2 { width: 58px; height: 58px; border-radius: 16px; }
        .auth-float.s3 { width: 64px; height: 64px; border-radius: 18px; }

        .auth-float.d1 { animation-duration: 7.2s; }
        .auth-float.d2 { animation-duration: 6.1s; }
        .auth-float.d3 { animation-duration: 8.0s; }
        .auth-float.d4 { animation-duration: 6.8s; }

        @keyframes authFloat {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .auth-card {
            position: relative;
            z-index: 1; /* above decor */
        }

        .auth-or {
            display: flex;
            align-items: center;
            gap: 12px;
            margin: 14px 0;
            color: rgba(15, 23, 42, 0.55);
            font-size: 12px;
            font-weight: 600;
        }
        .auth-or::before,
        .auth-or::after {
            content: "";
            height: 1px;
            flex: 1;
            background: rgba(15, 23, 42, 0.10);
        }

        .auth-social {
            display: flex;
            gap: 10px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 10px;
        }

        .auth-social a {
            width: 40px;
            height: 40px;
            border-radius: 12px;
            display: grid;
            place-items: center;
            border: 1px solid rgba(15, 23, 42, 0.10);
            background: rgba(255, 255, 255, 0.85);
            box-shadow: 0 8px 18px rgba(15, 23, 42, 0.06);
            color: rgba(15, 23, 42, 0.78);
            text-decoration: none;
            transition: transform 140ms ease, box-shadow 140ms ease;
        }

        .auth-social a:hover {
            transform: translateY(-1px);
            box-shadow: 0 14px 26px rgba(15, 23, 42, 0.10);
        }

        .auth-social a i {
            font-size: 16px;
        }

        .auth-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
            margin-top: 8px;
        }

        .auth-link {
            font-size: 12px;
            font-weight: 600;
            color: #2563eb;
            text-decoration: none;
        }
        .auth-link:hover { text-decoration: underline; }

        .auth-shell {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2.5rem 1rem;
        }

        .auth-card {
            width: 100%;
            max-width: 420px;
            border: 1px solid rgba(15, 23, 42, 0.08);
            border-radius: 16px;
            background: rgba(255, 255, 255, 0.92);
            box-shadow:
                0 24px 60px rgba(15, 23, 42, 0.12),
                0 2px 8px rgba(15, 23, 42, 0.06);
            backdrop-filter: blur(8px);
            overflow: hidden;
        }

        .auth-card-body {
            padding: 28px 28px 24px;
        }

        .auth-brand {
            display: flex;
            align-items: center;
            gap: 12px;
            justify-content: center;
            margin-bottom: 18px;
        }

        .auth-mark {
            width: 44px;
            height: 44px;
            border-radius: 12px;
            display: grid;
            place-items: center;
            background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%);
            box-shadow: 0 10px 22px rgba(37, 99, 235, 0.22);
            color: #fff;
            font-weight: 700;
            letter-spacing: -0.02em;
            user-select: none;
        }

        .auth-title {
            font-size: 18px;
            font-weight: 700;
            color: #0f172a;
            margin: 0;
            letter-spacing: -0.01em;
            line-height: 1.15;
        }

        .auth-subtitle {
            margin: 2px 0 0;
            font-size: 13px;
            color: rgba(15, 23, 42, 0.65);
        }

        .auth-form .form-label {
            font-size: 13px;
            font-weight: 600;
            color: rgba(15, 23, 42, 0.8);
            margin-bottom: 6px;
        }

        .auth-form .form-control,
        .auth-form .input-group-text {
            border-radius: 12px;
        }

        .auth-form .form-control {
            border: 1px solid rgba(15, 23, 42, 0.12);
            padding: 12px 12px;
            box-shadow: 0 1px 0 rgba(15, 23, 42, 0.02);
            transition: box-shadow 0.15s ease, border-color 0.15s ease, transform 0.15s ease;
        }

        .auth-form .form-control:focus {
            border-color: rgba(37, 99, 235, 0.55);
            box-shadow: 0 0 0 0.25rem rgba(37, 99, 235, 0.14);
        }

        .auth-primary {
            border: none;
            border-radius: 12px;
            padding: 12px 14px;
            font-weight: 700;
            letter-spacing: -0.01em;
            background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%);
            box-shadow: 0 14px 28px rgba(37, 99, 235, 0.18);
        }

        .auth-primary:hover {
            filter: brightness(0.98);
            transform: translateY(-1px);
        }

        .auth-primary:active {
            transform: translateY(0);
        }

        .auth-footer {
            padding: 14px 28px 22px;
            border-top: 1px solid rgba(15, 23, 42, 0.06);
            background: rgba(255, 255, 255, 0.7);
            text-align: center;
            font-size: 13px;
            color: rgba(15, 23, 42, 0.68);
        }

        .auth-footer a {
            color: #2563eb;
            font-weight: 600;
            text-decoration: none;
        }

        .auth-footer a:hover {
            text-decoration: underline;
        }

        /* Password strength helpers (auth pages) */
        .pw-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 10px;
            margin-top: 8px;
        }

        .pw-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            font-size: 12px;
            font-weight: 700;
            padding: 6px 10px;
            border-radius: 999px;
            border: 1px solid rgba(15, 23, 42, 0.10);
            background: rgba(255, 255, 255, 0.8);
            color: rgba(15, 23, 42, 0.78);
            user-select: none;
            white-space: nowrap;
        }

        .pw-badge.weak {
            border-color: rgba(239, 68, 68, 0.25);
            background: rgba(239, 68, 68, 0.08);
            color: rgb(185, 28, 28);
        }
        .pw-badge.medium {
            border-color: rgba(245, 158, 11, 0.25);
            background: rgba(245, 158, 11, 0.10);
            color: rgb(180, 83, 9);
        }
        .pw-badge.strong {
            border-color: rgba(34, 197, 94, 0.25);
            background: rgba(34, 197, 94, 0.10);
            color: rgb(21, 128, 61);
        }

        .pw-meter {
            flex: 1;
            height: 8px;
            border-radius: 999px;
            background: rgba(15, 23, 42, 0.08);
            overflow: hidden;
            min-width: 140px;
        }
        .pw-meter > span {
            display: block;
            height: 100%;
            width: 0%;
            border-radius: 999px;
            transition: width 160ms ease, background-color 160ms ease;
        }

        .pw-hints {
            margin: 8px 0 0;
            padding-left: 18px;
            color: rgba(15, 23, 42, 0.64);
            font-size: 12px;
        }
        .pw-hints li {
            margin: 2px 0;
        }

        /* ---------- Premium Add Employee Modal + Tabs ---------- */
        #addEmployeeModal .modal-dialog {
            max-width: 1200px;
        }

        #addEmployeeModal .modal-content {
            border: 1px solid rgba(15, 23, 42, 0.08);
            border-radius: 18px;
            overflow: hidden;
            box-shadow: 0 30px 80px rgba(15, 23, 42, 0.18);
            background: #fff;
        }

        #addEmployeeModal .modal-header {
            border: 0;
            padding: 18px 20px;
            background:
                radial-gradient(900px 260px at 10% 0%, rgba(99, 102, 241, 0.22), rgba(255,255,255,0)),
                radial-gradient(700px 240px at 80% 20%, rgba(139, 92, 246, 0.18), rgba(255,255,255,0)),
                linear-gradient(180deg, rgba(15, 23, 42, 0.02) 0%, rgba(15, 23, 42, 0.00) 100%);
        }

        #addEmployeeModal .modal-title {
            font-weight: 900;
            letter-spacing: -0.02em;
            color: #0f172a;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }

        #addEmployeeModal .modal-title::before {
            content: "";
            width: 10px;
            height: 10px;
            border-radius: 999px;
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            box-shadow: 0 10px 22px rgba(99, 102, 241, 0.25);
        }

        #addEmployeeModal .btn-close {
            opacity: 0.7;
        }
        #addEmployeeModal .btn-close:hover {
            opacity: 1;
        }

        #addEmployeeModal .modal-body {
            padding: 14px 20px 6px;
            background: #fff;
        }

        #addEmployeeModal .modal-footer {
            border-top: 1px solid rgba(15, 23, 42, 0.08);
            padding: 12px 20px;
            background: rgba(15, 23, 42, 0.02);
        }

        /* Tabs: premium pills */
        #addEmployeeTabs.nav-tabs {
            border: 0;
            gap: 8px;
            padding: 6px;
            background: rgba(15, 23, 42, 0.03);
            border: 1px solid rgba(15, 23, 42, 0.06);
            border-radius: 16px;
            overflow-x: auto;
            flex-wrap: nowrap;
            scrollbar-width: thin;
        }

        #addEmployeeTabs .nav-link {
            border: 0 !important;
            border-radius: 12px;
            padding: 10px 12px;
            font-size: 13px;
            font-weight: 800;
            color: rgba(15, 23, 42, 0.72);
            background: transparent;
            white-space: nowrap;
            transition: background-color 140ms ease, color 140ms ease, box-shadow 140ms ease, transform 140ms ease;
        }

        #addEmployeeTabs .nav-link:hover {
            color: rgba(15, 23, 42, 0.92);
            background: rgba(255, 255, 255, 0.75);
            box-shadow: 0 10px 22px rgba(15, 23, 42, 0.06);
            transform: translateY(-1px);
        }

        #addEmployeeTabs .nav-link.active {
            color: #0f172a;
            background: linear-gradient(135deg, rgba(99, 102, 241, 0.18) 0%, rgba(139, 92, 246, 0.16) 100%);
            box-shadow:
                inset 0 0 0 1px rgba(99, 102, 241, 0.25),
                0 14px 30px rgba(99, 102, 241, 0.14);
        }

        /* Tab content area: clean separation */
        #addEmployeeTabContent {
            margin-top: 14px;
            border: 1px solid rgba(15, 23, 42, 0.06);
            border-radius: 16px;
            padding: 14px;
            background: #fff;
        }

        /* Inputs inside Add Employee modal */
        #addEmployeeModal .form-label {
            font-size: 12px;
            font-weight: 800;
            letter-spacing: 0.02em;
            color: rgba(15, 23, 42, 0.75);
            margin-bottom: 6px;
        }

        #addEmployeeModal .form-control,
        #addEmployeeModal .form-select,
        #addEmployeeModal .input-group-text,
        #addEmployeeModal textarea {
            border-radius: 12px;
        }

        #addEmployeeModal .form-control,
        #addEmployeeModal .form-select,
        #addEmployeeModal textarea {
            border: 1px solid rgba(15, 23, 42, 0.12);
            box-shadow: 0 1px 0 rgba(15, 23, 42, 0.02);
            transition: box-shadow 140ms ease, border-color 140ms ease;
        }

        #addEmployeeModal .form-control:focus,
        #addEmployeeModal .form-select:focus,
        #addEmployeeModal textarea:focus {
            border-color: rgba(99, 102, 241, 0.55);
            box-shadow: 0 0 0 0.25rem rgba(99, 102, 241, 0.14);
        }

        /* Wizard buttons */
        #addEmployeeModal .modal-footer .btn {
            border-radius: 12px;
            font-weight: 800;
            padding: 10px 14px;
        }
        #addEmployeeModal .modal-footer .btn-secondary {
            background: rgba(15, 23, 42, 0.08);
            border: 1px solid rgba(15, 23, 42, 0.10);
            color: rgba(15, 23, 42, 0.82);
        }
        #addEmployeeModal .modal-footer .btn-secondary:hover {
            background: rgba(15, 23, 42, 0.12);
        }
        #addEmployeeModal .modal-footer .btn-primary {
            border: none;
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            box-shadow: 0 14px 28px rgba(99, 102, 241, 0.20);
        }
        #addEmployeeModal .modal-footer .btn-primary:hover {
            filter: brightness(0.98);
        }
    </style>
</head>
<body class="{{pageClass}}">
    {{{body}}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script>
        // Default API base is same-origin (works for localhost + production deployments)
        const API_BASE = `${window.location.origin.replace(/\/$/, '')}/api/v1`;

        // Helper: detect if current page is login or register
        function isAuthPage() {
            const p = window.location.pathname.toLowerCase();
            return p === '/login' || p === '/register' || p.endsWith('/login') || p.endsWith('/register');
        }
        
        // Helper function to make API calls (minimal fixes only)
        async function apiCall(endpoint, options = {}) {
            const token = localStorage.getItem('accessToken') || null;

            const headers = {
                'Content-Type': 'application/json',
                ...(options.headers || {})
            };

            // Attach Authorization only if we have a real token and caller didn't skip auth
            if (token && !options.skipAuth) {
                headers['Authorization'] = `Bearer ${token}`;
            }

            const defaultOptions = {
                credentials: 'include',
                ...options,
                headers
            };
            
            try {
                const response = await fetch(`${API_BASE}${endpoint}`, defaultOptions);

                // If unauthorized:
                if (!response.ok) {
                    const errorData = await response.json().catch(() => ({}));

                    const isAuthError =
                        response.status === 401 ||
                        (errorData.error && (
                            String(errorData.error).includes('jwt expired') ||
                            String(errorData.error).includes('Invalid access Token') ||
                            String(errorData.error).includes('Authentication failed')
                        ));

                    if (isAuthError) {
                        // Only redirect to /login if we are NOT already on login/register
                        if (!isAuthPage()) {
                            console.log('Authentication failed, redirecting to login...');
                            localStorage.removeItem('accessToken');
                            localStorage.removeItem('user');
                            window.location.replace('/login');
                            return;
                        }
                        // If we are on an auth page, just return the error JSON to the caller
                        return errorData;
                    }

                    // Non-auth failure: throw with message for the caller
                    throw new Error(errorData.message || errorData.error || 'API request failed');
                }
                
                return await response.json();
            } catch (error) {
                // Network or other errors; don't force redirect if already on auth pages
                const msg = String(error && error.message || '');
                if (
                    (msg.includes('jwt expired') || msg.includes('Invalid access Token') || msg.includes('Authentication failed')) &&
                    !isAuthPage()
                ) {
                    console.log('Authentication failed, redirecting to login...');
                    localStorage.removeItem('accessToken');
                    localStorage.removeItem('user');
                    window.location.replace('/login');
                    return;
                }
                throw error;
            }
        }
        
        // Check authentication
        function checkAuth() {
            const token = localStorage.getItem('accessToken');
            const currentPath = window.location.pathname;
            
            // Only redirect if we're not already on login/register pages and no token exists
            if (!token && 
                currentPath !== '/login' && 
                currentPath !== '/register' && 
                !currentPath.includes('/login') && 
                !currentPath.includes('/register')) {
                console.log('No token found, redirecting to login...');
                // Use replace to prevent back button issues
                window.location.replace('/login');
            }
        }
        
        // Logout function
        function logout() {
            localStorage.removeItem('accessToken');
            localStorage.removeItem('user');
            window.location.replace('/login');
        }
        
        // Menu toggle functionality
        function toggleMenu(menuId) {
            const submenu = document.getElementById(menuId);
            const toggle = document.querySelector(`[onclick="toggleMenu('${menuId}')"]`);
            
            if (submenu.classList.contains('show')) {
                submenu.classList.remove('show');
                toggle.classList.add('collapsed');
            } else {
                submenu.classList.add('show');
                toggle.classList.remove('collapsed');
            }
        }
        
        // Initialize auth check (only on protected pages)
        const currentPath = window.location.pathname;
        if (currentPath !== '/login' && currentPath !== '/register' && !window.authChecked) {
            window.authChecked = true;
            checkAuth();
        }
    </script>
</body>
</html>
